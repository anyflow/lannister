http://normanmaurer.me/presentations/2014-facebook-eng-netty/slides.html#21.0
DefaultByteBufHolder

http://normanmaurer.me/presentations/2014-facebook-eng-netty/slides.html#25.0
Sharing the same EventLoopGroup

Pooled-ByteBuf


http://normanmaurer.me/presentations/2014-facebook-eng-netty/slides.html#30.0
Annotate ChannelHandler that are stateless with @ChannelHandler.Shareable and use the same instance accross Channels to reduce GC.

http://normanmaurer.me/presentations/2014-facebook-eng-netty/slides.html#32.0
 byte[] array = out.array(); 
    int offset = out.arrayOffset() + out.writerIndex();
    out.writeIndex(out.writerIndex() + encode(msg, array, offset)); 
  }


  http://normanmaurer.me/presentations/2014-facebook-eng-netty/slides.html#33.0
  channel.config().setAutoRead(false); 
channel.read(); 
channel.config().setAutoRead(true); 